import Video from '@/layouts/components/Video';

function Watch({ data }) {
    return (
        <div className="px-3.5">
            <Video data={data} />
        </div>
    );
}

export default Watch;
